---
# ========================================
# cPanel Git Deployment Configuration
# ========================================
# 
# Acest fișier instruiește cPanel CE să:
# 1. Clone/Pull din GitHub
# 2. Copieze DOAR wp-content/ în locația corectă
# 3. Ignore tot restul (WordPress core, documentație, etc.)
#
# Deployment automat: cPanel → Git Version Control → Pull/Deploy
# ========================================

deployment:
  tasks:
    # ========================================
    # TASK 1: Backup înainte de deploy (SAFETY)
    # ========================================
    - export TIMESTAMP=$(date +%Y%m%d_%H%M%S)
    - export BACKUP_DIR=$HOME/backups
    - mkdir -p $BACKUP_DIR
    - echo "💾 Creating backup..."
    - tar -czf $BACKUP_DIR/wp-content-backup-$TIMESTAMP.tar.gz $HOME/public_html/test/wp-content 2>/dev/null || true
    - echo "✅ Backup saved: wp-content-backup-$TIMESTAMP.tar.gz"
    
    # ========================================
    # TASK 2: Deploy PLUGINS (Sursă adâncă: app/public/wp-content/)
    # ========================================
    - echo "🔄 Deploying plugins from deep path..."
    
    # Plugin 1: webgsm-b2b-pricing
    - /bin/cp -rf $HOME/repositories/webgsm/app/public/wp-content/plugins/webgsm-b2b-pricing $HOME/public_html/test/wp-content/plugins/
    
    # Plugin 2: webgsm-checkout-pro
    - /bin/cp -rf $HOME/repositories/webgsm/app/public/wp-content/plugins/webgsm-checkout-pro $HOME/public_html/test/wp-content/plugins/
    
    # Plugin 3: webgsm-customer-tiers
    - /bin/cp -rf $HOME/repositories/webgsm/app/public/wp-content/plugins/webgsm-customer-tiers $HOME/public_html/test/wp-content/plugins/
    
    - echo "✅ Plugins deployed!"
    
    # ========================================
    # TASK 3: Deploy THEME (Sursă adâncă: app/public/wp-content/)
    # ========================================
    - echo "🔄 Deploying theme from deep path..."
    - /bin/cp -rf $HOME/repositories/webgsm/app/public/wp-content/themes/martfury-child $HOME/public_html/test/wp-content/themes/
    - echo "✅ Theme deployed!"
    
    # ========================================
    # TASK 4: Cleanup (minimal - fără find)
    # ========================================
    - echo "🧹 Cleanup skipped (manual if needed)"
    
    # ========================================
    # TASK 5: Set permissions (doar pe foldere noi)
    # ========================================
    - echo "🔒 Setting permissions..."
    
    # Doar pe plugin-urile custom (fără recursiv find)
    - chmod -R 755 $HOME/public_html/test/wp-content/plugins/webgsm-b2b-pricing 2>/dev/null || true
    - chmod -R 755 $HOME/public_html/test/wp-content/plugins/webgsm-checkout-pro 2>/dev/null || true
    - chmod -R 755 $HOME/public_html/test/wp-content/plugins/webgsm-customer-tiers 2>/dev/null || true
    - chmod -R 755 $HOME/public_html/test/wp-content/themes/martfury-child 2>/dev/null || true
    
    - echo "✅ Permissions set!"
    
    # ========================================
    # TASK 6: Clear WordPress cache & Force plugin reload
    # ========================================
    - echo "🗑️ Clearing cache & forcing plugin reload..."
    
    # Șterge cache-ul de transients/object cache
    - rm -rf $HOME/public_html/test/wp-content/cache/* 2>/dev/null || true
    
    # Touch plugin file pentru a forța reload
    - touch $HOME/public_html/test/wp-content/plugins/webgsm-b2b-pricing/webgsm-b2b-pricing.php
    
    # WP-CLI cache flush (dacă ai WP-CLI instalat)
    - cd $HOME/public_html/test && wp cache flush 2>/dev/null || echo "⚠️ WP-CLI not available (manual cache clear needed)"
    
    # ========================================
    # FINAL: Success message
    # ========================================
    - echo ""
    - echo "========================================="
    - echo "✅ DEPLOYMENT COMPLETE!"
    - echo "========================================="
    - echo "📦 Plugins deployed:"
    - echo "   - webgsm-b2b-pricing"
    - echo "   - webgsm-checkout-pro"
    - echo "   - webgsm-customer-tiers"
    - echo ""
    - echo "🎨 Theme deployed:"
    - echo "   - martfury-child"
    - echo ""
    - echo "💾 Backup location:"
    - echo "   $BACKUP_DIR/wp-content-backup-$TIMESTAMP.tar.gz"
    - echo ""
    - echo "🌐 Check your site:"
    - echo "   https://your-domain.com"
    - echo "========================================="
