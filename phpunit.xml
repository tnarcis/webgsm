<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.6/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         convertErrorsToExceptions="true"
         convertNoticesToExceptions="true"
         convertWarningsToExceptions="true"
         stopOnFailure="false"
         verbose="true">

    <!-- Test Suites -->
    <testsuites>
        <testsuite name="unit">
            <directory>tests/php/unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/php/integration</directory>
        </testsuite>
    </testsuites>

    <!-- Code Coverage -->
    <coverage processUncoveredFiles="true">
        <include>
            <directory suffix=".php">plugins/webgsm-checkout-pro/includes</directory>
            <directory suffix=".php">themes/martfury-child/includes</directory>
        </include>
        <exclude>
            <directory>vendor</directory>
            <directory>tests</directory>
        </exclude>
        <report>
            <html outputDirectory="coverage/html"/>
            <text outputFile="php://stdout" showUncoveredFiles="false"/>
        </report>
    </coverage>

    <!-- PHP Settings -->
    <php>
        <env name="WP_TESTS_DIR" value="/tmp/wordpress-tests-lib"/>
        <env name="WP_CORE_DIR" value="/tmp/wordpress"/>
        <const name="WP_DEBUG" value="true"/>
        <const name="WP_DEBUG_LOG" value="false"/>
        <const name="WP_DEBUG_DISPLAY" value="true"/>
        <const name="SCRIPT_DEBUG" value="true"/>
        <const name="WEBGSM_TESTING" value="true"/>
    </php>

    <!-- Logging -->
    <logging>
        <junit outputFile="tests/results/junit.xml"/>
    </logging>
</phpunit>
